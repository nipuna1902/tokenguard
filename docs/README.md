# TokenGuard documentation

Start with the [documentation guide](DOCUMENTATION.md). It links to the current architecture, pricing, operations, methodology, and product references.

Runtime behavior in `app/`, `components/`, `lib/`, and `types/` is authoritative.
